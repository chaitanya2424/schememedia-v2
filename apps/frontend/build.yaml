targets:
  $default:
    builders:
      json_serializable:
        options:
          # Backend wire format is snake_case; Dart fields stay camelCase.
          # This one setting means no per-field @JsonKey(name: '...') across
          # every DTO in the app.
          field_rename: snake
          explicit_to_json: true
